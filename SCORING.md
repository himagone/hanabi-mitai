# スコアリングロジック

花火の観覧地点としての適性を 0〜100 点で評価する。

## 全体構造

```
totalScore = quality × occlusion × distanceVisibility
```

3つの乗算項からなる。**遮蔽(occlusion) は乗算項**である点が重要で、
見えなければ（occlusion=0）他がいくら満点でも総合点は0になる。

- **quality（見え方の質）**: 遮蔽を除く4項目の重み付き和を再正規化（0〜1）
- **occlusion（遮蔽＝可視割合）**: 開花球のうち遮蔽されずに見えている縦割合（0〜1）
- **distanceVisibility（距離乗数）**: 見かけサイズ×大気減衰（0〜1）

> **なぜ遮蔽を乗算にするか**: 旧式は遮蔽を重み0.35の**加算項**にしていたため、
> 「一発も見えない高台の公園」が「全部見える住宅地」より高得点になる逆転が起きた。
> 遮蔽は加算されるべき性質ではなく、他項目に掛かる性質のため乗算項に移した。

## 評価項目と重み

### 質(quality) の重み（合計 0.65 で再正規化して 0〜1 化）

| 項目 | 重み | 説明 |
|------|------|------|
| 仰角 (viewingAngle) | 0.25 | 花火を見上げる角度が最適か |
| 標高 (elevation) | 0.15 | 打上地点より高い位置にあるか |
| 場所 (accessibility) | 0.15 | 公園・広場など観覧しやすい場所か |
| 勾配 (slope) | 0.10 | 花火方向に開けた斜面か |

```
quality = (0.25·仰角 + 0.15·標高 + 0.10·勾配 + 0.15·場所) / 0.65
```

### 乗算項

| 項目 | 説明 |
|------|------|
| 視線 (lineOfSight / occlusion) | 開花球の可視割合。地形・建物の遮蔽を統合 |
| 距離視認性 (distanceVisibility) | 見かけサイズ×大気減衰 |

建物データ未取得時は地形のみで可視割合を出し、不確実性として ×0.9 する。

## 各項目の詳細

### 距離視認性 (distanceVisibility)

花火の見かけの大きさと大気による減衰を考慮した乗数。全体スコアに掛け合わせる。

**見かけサイズスコア:**

花火の開花直径と距離から見かけの角度を計算。

```
見かけ角度(°) = atan2(開花直径, 距離) × 180 / π
sizeScore      = 1 − exp(−見かけ角度 / 8)      // 飽和なし・0〜1
```

- **近い（大きく見える）ほど単調に高い**。頭打ちにしないため、近距離の没入視が上位化する。
- 仰角の"好み"を廃止したので、距離の効き＝この見かけサイズが担う。
- 例: 10号 400m≈0.99 / 1km≈0.88 / 2km≈0.66、小玉(D90) 400m≈0.80 / 1km≈0.47。

**大気減衰スコア:**

```
atmosphericScore = exp(-max(0, 距離 - 3000) / 10000)
```

- 3km 以内: ペナルティなし
- それ以降: 指数的に減衰（晴天でも 15km 以上はかすみで劣化）

**最終値:**

```
distanceVisibility = sizeScore × atmosphericScore
```

**花火の開花直径:**

大会ごとに指定可能。指定がない場合はデフォルト 150m（5号玉相当）。

大会ごとに `data-diameter` 属性で指定。詳細は [README.md](./README.md) の対応花火大会を参照。

### 開花球モデル

花火は点でなく直径 D の球として扱う。仰角・視線の両方でこの縦の広がりを用いる。

```
開花高度 ≈ 開花直径     // 実測: 号数×約33m(高度) ≒ 号数×約32m(直径)
中心高度 = 打上地点標高 + 直径 × 1.05
半径 r   = 直径 / 2
```

例: 10号(直径約320m)なら中心約336m。旧式 `30 + 1.1×直径` は大玉・小玉とも過大だった。

### 仰角スコア (viewingAngle)

開花球の下端・中心・上端への見上げ角を平均し、最適角で評価する。

```
角度_k = atan2(高度_k − 観覧者の目の高さ, 水平距離)   (高度_k ∈ {下端,中心,上端})
score  = gauss(mean(角度_下端, 角度_中心, 角度_上端))
```

- 観覧者の目の高さ = 地面標高 + 1.5m
- 最適角度: 35°（ガウス分布、σ=25°）
- 球の上端すら視点以下の場合: 0.05（ほぼ最低点）

### 標高スコア (elevation)

```
相対標高 ≥ 0     → 1.0（打上と同じ高さ以上は理想）
相対標高 < 0     → max(0.6, 1.0 + 相対標高/60)（低い分だけ緩やか減点、-24mで0.6）
```

- 高台の利点（遮蔽回避）は可視率(occlusion)が直接評価するため、正側は満点で頭打ちにする。
- **平坦・打上と同レベルの開けた河川敷を 0.5 で頭打ちにしていた旧シグモイドを廃止**
  （理想的な観覧地が総合80点で頭打ちになる原因だった）。

### 視線スコア (lineOfSight) ＝ 2D可視率 (occlusion)

開花球を「観覧者に正対する円盤」とみなし、**方位(横)×高さ(縦)の格子**で
遮られずに見えているセル率を返す（0〜1）。縦だけでなく横も見るため、
**完全に見える(1.0) / 建物の隙間から見える(0<f<1) / 見えない(0)** を区別できる。
この値がそのまま乗算項 occlusion。

**格子（開花半径 r に対する比）:**
- 横（方位）: `u ∈ {-0.8,-0.4,0,0.4,0.8}`（視線に直交する水平オフセット `u·r` メートル）
- 縦（高さ）: `v ∈ {-0.7,0,0.7}`（セル標高 = 中心標高 + `v·r`）
- 円盤マスク: `u²+v² ≤ 1` のセルのみ対象

**各方位（横オフセット u）のレイで遮蔽越えに必要な最低標高を求める:**

1. 打上位置を視線直交方向へ `u·r` m ずらした標的へレイを飛ばす
2. そのレイ上の遮蔽点（水平距離比 t、遮蔽物頂上標高 O）を集める
   - 地形: 40m 間隔サンプリングの地面標高
   - 建物: レイと建物ポリゴンの交差点で 地面標高 + 建物高さ
3. `H_block(u) = max[ 目の高さ + (O − 目の高さ) / t ]`（手前の遮蔽物ほど強く効く）

**可視率:**
```
可視率 = 「セル標高(v) ≥ H_block(u)」を満たす円盤内セル数 / 円盤内セル総数
```

- 全方位クリア → 1.0 ／ 中心が塞がっても端の方位が開けば 0<可視率<1（隙間から見える）
- 開花球が小さく遠いほど角度サイズが小さく、判定はほぼ二値（＝物理的に妥当）
- 観覧者の目の高さ = 地面標高 + 1.5m

**建物高さ（PLATEAU の実測 measuredHeight を優先 / 圏外は OSM 推定）:**

PLATEAU 圏内（東京23区）は建物ごとの実測高さ、圏外は OSM 推定にフォールバック:

| 建物タイプ | デフォルト高さ |
|------------|----------------|
| apartments / residential | 10m |
| house / detached | 7m |
| commercial / office | 15m |
| industrial / warehouse | 8m |
| garage | 3m |
| その他 | 8m |

OSM は `height` / `building:levels` タグがあれば優先（1階=3m で換算）。

**建物データ未取得時:**

地形のみで可視割合を出し、不確実性として ×0.9 する。表示用の内訳値
`ScoreBreakdown.lineOfSight` は -1（フロント表示「周辺データ不足」）。

### 勾配スコア (slope)

地形の傾きが花火方向に向いているか（開けた斜面か）を評価。

**方法:**

1. 観覧地点の東西南北 ±33m の標高から勾配ベクトルを計算
2. 花火方向の単位ベクトルとの内積で方向の一致度を判定

**スコア:**

- 平坦・花火方向に下る（開けた）斜面: 1.0（理想）
- 花火方向に上る斜面（遮られやすい）: 0.5〜1.0（真上りで0.5）
- ※ 平坦を 0.5 で頭打ちにしていた旧式を廃止（理想地の総合80点頭打ちの一因）

### 場所スコア (accessibility)

OSM の土地利用データから観覧のしやすさを判定。副次要素なので、不明でも減点しない。

| 場所 | スコア |
|------|--------|
| 公園・広場・河川敷・遊び場 | 1.0 |
| 住宅地 | 0.7 |
| データなし / その他・不明 | 0.9 |

> 旧式は「データなし=0.6・住宅地=0.3」で、Overpass 失敗時に全地点が 0.6 に落ち
> 総合80点頭打ちの主因になっていた。見えるかは可視率・仰角・距離が決めるため、
> 場所は軽い加点要素に留める。

## 立入禁止（保安）ゾーン

打上地点まわりは絶対に立入禁止のため、**保安半径 = max(開花直径, 150m)** の円を
自動で赤く表示し、常に安全側に扱う（黙って削除せず可視化）。

- グリッド分析: 保安圏内の候補は採点対象から除外（`totalPositions` に出さない）。
- 単一地点採点(モバイル): 保安圏内をタップした場合は「立入禁止エリアです」で拒否。
- フロント/バックエンドで同一式。利用者が手動で引く立入禁止エリアと併用する。

## 2パス方式＝分枝限定法（デスクトップのグリッド分析）

大量の候補地点を、取りこぼしなく効率的に評価する。

### パス1: 上限値 (quickScoreUB)

ネットワーク不要の3項目（仰角・標高・場所）を計算し、**未計算の勾配・遮蔽を満点(1.0)と
仮定した上限値**を出す。全候補をこの上限値の降順でソートする。

```
qualityUB     = (0.25·仰角 + 0.15·標高 + 0.10·1.0 + 0.15·場所) / 0.65   // 勾配=1と仮定
quickScoreUB  = qualityUB × 1.0(遮蔽=1と仮定) × distanceVisibility
```

> **なぜ上限値か**: 旧式はネットワーク不要の3項目の**下限的な概算**で上位100件に絞ったため、
> 遮蔽の良い地点が本採点前に構造的に捨てられていた（探索の足切りを下限で行う誤り）。

### パス2: 本採点＋打ち切り

上限値の降順に本採点（視線・勾配を追加）し、real total を計算する。
上位K件（K=10）の real を保持し、**現在の候補の上限値がK位の real を下回った時点で打ち切る**。

```
for 候補 in 上限値降順:
  if 本採点済み ≥ K かつ 候補.上限値 ≤ K位のreal: break   // 残りは上位K入り不可
  本採点(候補)
```

`real ≤ 上限値` が常に成立するため、この打ち切りは**取りこぼしゼロ**（分枝限定法）。
地形・建物・DEM はエリア単位で事前取得済みのため、本採点はCPU＋キャッシュ参照のみで高速
（2600点規模でも1秒未満）。ヒートマップは本採点済み=real、未採点=上限値で描画する。

## データソース

| データ | ソース | 用途 |
|--------|--------|------|
| 標高 | 国土地理院 DEM5A/DEM10B（ズーム15、256×256 テキストタイル、並列取得） | 標高スコア、仰角、視線（地形遮蔽）、勾配 |
| 土地利用 | OpenStreetMap Overpass API | 場所スコア |
| 建物（優先） | PLATEAU 建物 MVT（実測高さ measuredHeight、東京23区、ズーム14） | 視線チェック（ポリゴン交差判定） |
| 建物（フォールバック） | OpenStreetMap Overpass API（PLATEAU 圏外・推定高さ） | 視線チェック（ポリゴン交差判定） |

**可視化レイヤー（フロントエンド）**: 地図右上の「3D」トグルで、PLATEAU 3D 建物
（実測高さで押し出し）と地理院 DEM 地形（起伏・陰影）を重ねて表示し、遮蔽の理由を
目視確認できる。

## おすすめ理由の生成

スコアの内訳と数値から短い説明を生成する。

| 条件 | 表示 |
|------|------|
| distance < 0.05 | 「遠すぎて視認困難」 |
| distance < 0.3 | 「距離が遠い」 |
| accessibility >= 0.9 | 「公園・広場」 |
| accessibility <= 0.2 | 「住宅地」 |
| lineOfSight < 0 | 「周辺データ不足」 |
| lineOfSight >= 0.9 | 「視界が開けている」 |
| lineOfSight >= 0.5 | 「一部障害物あり」 |
| lineOfSight < 0.3 | 「建物が視界を遮る」 |
| viewingAngle 25°〜45° | 「見上げやすい」 |
| viewingAngle 10°〜25° | 「正面に見える」 |
| relativeElevation > 10m | 「+○m」 |
| relativeElevation > 3m | 「やや高台」 |
| slope > 0.7 | 「開けた方角」 |
